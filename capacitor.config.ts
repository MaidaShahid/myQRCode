import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'my-qrcode',
  webDir: 'dist/my-qrcode',
  bundledWebRuntime: false
};

export default config;
